//method chaining = calling one method after another in on continuous
//  line of code

let userName = "bro";

// Grabs the letter at index 0 and changes it to uppercase
let letter = userName.charAt(0).toLocaleUpperCase();

console.log(letter);